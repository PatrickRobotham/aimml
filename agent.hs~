import Context

-- The agent needs a model, ignore this for now
data Agent = Agent {
  age :: Int,
  totalReward :: Double,
  
  }
             
averageReward :: Agent -> Double
averageReward x = if age x > 0
                  then (totalReward x) / (fromIntegral $ age x)
                  else 0

